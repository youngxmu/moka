<!DOCTYPE html>
<html>
  <head>
    <%- include common/meta.ejs %>
    <title>国防教育动态</title>
    <link rel='stylesheet' href='css/index/list.css' >
  </head>
  <body>
    <%- include common/header.ejs %>
    <div class="container">
      <ol class="breadcrumb" style="border-radius:0;">
        <li>
          <a href="index">首页</a>
        </li>
        <li>
          <a href="index/nlist">国防教育动态</a>
        </li>
        <% if(typeof subtitle != 'undefined'){%>
        <li class="active">
          <%= subtitle%>
        </li>
        <% } %>
      </ol>
      <div class="main">
        <div class="res-list">
          <span class="res-title">标题</span>
          <span>作者</span>
          <span>时间</span>
          <span></span>
        </div>
        <div id="top_list" style="border-bottom: 1px dashed #cccccc;"></div>
        <div id="resource_list"></div>
        <div id="pagebar"></div>
      </div>
    </div>
    <%- include common/footer.ejs %>

    <script type="text/template" id="resource-tpl">
    {@each list as resource}
    <div class="res-list">
      <span class="res-title">
        <a href="index/news/${resource.id}" >${resource.title}</a>
      </span>
      <span>{@if resource.author}${resource.author}{@else}admin{@/if}</span>
      <span>${resource.create_time}</span>
      <span></span>
    </div>
    {@/each}
    </script>
    <script type="text/javascript" src="js/index/news-list.js"></script>
    <script>
      $(document).ready(function() {
          moka.index.news.init();
      });
    </script>
  </body>
</html>
